<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.budgetservice.mapper.ExpenseMapper">
    <select id="findByUserId" parameterType="long" resultType="ExpenseDto">
        select history_id, amount, category, payment_method, outlay_datetime, source, user_id
        from MONEYHISTORY
        where user_id = #{userId};
    </select>
</mapper>